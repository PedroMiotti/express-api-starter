import type { PrismaClient } from '../../../src/infra/database/generated/prisma/client';

export const seedUsers = async (_prisma: PrismaClient): Promise<void> => {
  // Add user seed data here once User model is available.
};
