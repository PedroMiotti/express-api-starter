import type { PrismaClient } from '../../../src/infra/database/generated/prisma/client';

export const seedOrganizations = async (
  _prisma: PrismaClient,
): Promise<void> => {
  // Add organization seed data here once Organization model is available.
};
